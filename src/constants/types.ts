/*eslint-disable*/
export enum EOperators {
    SQL_CLONE = `SQL_CLONE`,
    SQL_EXTRACT = `SQL_EXTRACT`,
    SQL_LOAD = `SQL_LOAD`,
    JOIN = `JOIN`,
    CALCULATED = `CALCULATED`,
}
/*eslint-enable*/
